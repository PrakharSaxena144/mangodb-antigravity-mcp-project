[project]
name = "inventory-mcp-server"
version = "0.2.0"
description = "MCP inventory server backed by local MongoDB, for use with Antigravity IDE"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
    "pymongo>=4.15",
    "dnspython>=2.6.0",
]
